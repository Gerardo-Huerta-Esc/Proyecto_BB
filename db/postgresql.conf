# PostgreSQL Configuration (inline en docker-compose.yml)
#
# En este proyecto, los parámetros de configuración de PostgreSQL
# se pasan directamente como argumentos al comando de inicio en
# docker-compose.yml:
#
# command: >
#   postgres
#   -c shared_buffers=256MB
#   -c work_mem=16MB
#   -c wal_compression=on
#
# Esta configuración inline es más simple para proyectos dockerizados
# y evita tener que montar un archivo postgresql.conf completo.
#
# Para verificar los valores actuales, ejecutar:
#   docker exec payments-postgres psql -U admin -d payments -c "SHOW shared_buffers;"
#   docker exec payments-postgres psql -U admin -d payments -c "SHOW work_mem;"
#   docker exec payments-postgres psql -U admin -d payments -c "SHOW wal_compression;"
